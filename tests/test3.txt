fn main() {
    fn fib: int (n: int, a: bool) {
        let a, b: int;
        let tmp, i: int;
        a = 0;
        b = 1;
        i = 0;
        while (i <= n) {
            tmp = a;
            a = b;
            b = tmp + b / 3;
            i = i + 1;
        }

        if (x < 0) {

        }

        proc procedimento() {
            
        }

        procedimento();
    }

    let resultado: int;
    let booleano: bool;
    booleano = true;
    fib(10);
    write(true);
}
